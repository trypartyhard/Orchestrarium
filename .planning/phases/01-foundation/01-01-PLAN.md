---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/main.rs
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
  - src-tauri/tauri.conf.json
  - package.json
  - src/main.tsx
  - src/App.tsx
autonomous: true
requirements: [SCAN-01]

must_haves:
  truths:
    - "cargo tauri dev launches the app window without errors"
    - "Tauri capabilities include fs:scope for $HOME/.claude/**"
    - "requireLiteralLeadingDot is set to false in tauri.conf.json"
  artifacts:
    - src-tauri/Cargo.toml
    - src-tauri/capabilities/default.json
    - src-tauri/tauri.conf.json
  key_links:
    - "Cargo.toml references gray_matter, serde, serde_json, tauri-specta, specta, specta-typescript, tokio"
---

<objective>
Scaffold the Tauri 2 project with React+TypeScript template and configure all permissions/capabilities correctly from day one.

Purpose: Foundation setup that prevents pitfalls C-1 (missing scope), C-2 (dotfolder access blocked), and M-6 (wrong Mutex type). Every subsequent plan depends on this being correct.
Output: Working Tauri 2 project that builds and launches with correct FS permissions configured.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Tauri 2 project with React+TypeScript</name>
  <files>package.json, src-tauri/Cargo.toml, src-tauri/src/main.rs, src-tauri/src/lib.rs, src/main.tsx, src/App.tsx</files>
  <action>
Run `npm create tauri-app@latest . -- --template react-ts` in the project root (C:/Users/User/Desktop/Claude-Agent-Manager). If the directory has existing files (.planning/, .git/), scaffold in a temp directory and move the generated files over, preserving .planning/ and .git/.

After scaffolding:
1. Add Rust dependencies to `src-tauri/Cargo.toml`:
   ```toml
   [dependencies]
   gray_matter = "0.3"
   serde = { version = "1", features = ["derive"] }
   serde_json = "1"
   tokio = { version = "1", features = ["sync"] }
   tauri-specta = { version = "=2.0.0-rc.21", features = ["derive", "typescript"] }
   specta = { version = "=2.0.0-rc.22", features = ["derive"] }
   specta-typescript = "0.0.9"
   dirs = "6"
   tauri-plugin-fs = "2"
   ```

2. Add frontend dependencies:
   ```bash
   npm install zustand@5 lucide-react
   npm install -D tailwindcss @tailwindcss/vite
   ```

3. Replace the default `src/App.tsx` with a minimal placeholder:
   ```tsx
   function App() {
     return <div className="bg-[#12121c] text-[#d0d0e8] min-h-screen p-4">
       <h1 className="text-xl">Claude Agent Manager</h1>
       <p className="text-[#555577]">Phase 1: Foundation</p>
     </div>
   }
   export default App
   ```

4. In `src-tauri/src/lib.rs`, set up the Tauri builder with tauri-plugin-fs:
   ```rust
   pub fn run() {
       tauri::Builder::default()
           .plugin(tauri_plugin_fs::init())
           .run(tauri::generate_context!())
           .expect("error while running tauri application");
   }
   ```
  </action>
  <verify>
    <automated>cd src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Project scaffolded, all Rust deps resolve, cargo check passes</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tauri capabilities and permissions</name>
  <files>src-tauri/capabilities/default.json, src-tauri/tauri.conf.json</files>
  <action>
1. Create/update `src-tauri/capabilities/default.json`:
   ```json
   {
     "$schema": "../gen/schemas/desktop-schema.json",
     "identifier": "default",
     "description": "Default capabilities for CAM",
     "windows": ["main"],
     "permissions": [
       "core:default",
       "fs:default",
       "fs:allow-read-dir",
       "fs:allow-read-file",
       "fs:allow-read-text-file",
       "fs:allow-rename",
       "fs:allow-create",
       "fs:allow-exists",
       "fs:allow-remove",
       {
         "identifier": "fs:scope",
         "allow": [
           { "path": "$HOME/.claude/**" }
         ]
       }
     ]
   }
   ```

2. In `src-tauri/tauri.conf.json`, add the fs plugin config to allow dotfolder access:
   ```json
   "plugins": {
     "fs": {
       "requireLiteralLeadingDot": false
     }
   }
   ```

3. Set app metadata in `tauri.conf.json`:
   - `productName`: "Claude Agent Manager"
   - `identifier`: "com.cam.app"
   - Window title: "Claude Agent Manager"
   - Window size: 1200x800, minWidth: 800, minHeight: 600
  </action>
  <verify>
    <automated>cd src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Capabilities file exists with fs:scope for $HOME/.claude/**, requireLiteralLeadingDot is false, app metadata configured</done>
</task>

<task type="auto">
  <name>Task 3: Verify full build and app launch</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
1. Run `cargo tauri dev` to verify the app launches (use --no-watch to avoid hanging):
   - App window should open with dark background (#12121c)
   - No permission errors in console
   - Close the app after verification

2. Run `cd src-tauri && cargo test` to ensure test infrastructure works (even with no tests yet, the framework should compile)

3. Verify tauri-specta setup compiles by adding a minimal command in lib.rs:
   ```rust
   #[tauri::command]
   #[specta::specta]
   fn greet() -> String {
       "CAM is running".to_string()
   }
   ```
   Wire it into the builder with `invoke_handler(tauri::generate_handler![greet])`.
  </action>
  <verify>
    <automated>cd src-tauri && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>cargo build succeeds, app launches without errors, specta command compiles</done>
</task>

</tasks>

<verification>
1. `cargo check` passes in src-tauri/
2. `src-tauri/capabilities/default.json` contains fs:scope with $HOME/.claude/**
3. `tauri.conf.json` has requireLiteralLeadingDot: false
4. Cargo.toml includes gray_matter, serde, tokio, tauri-specta, specta, dirs
</verification>

<success_criteria>
- Tauri 2 project scaffolded with React+TS template
- All Rust dependencies resolve and compile
- FS capabilities configured with correct scope for ~/.claude/
- Dotfolder access enabled (requireLiteralLeadingDot: false)
- App builds and can launch without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
