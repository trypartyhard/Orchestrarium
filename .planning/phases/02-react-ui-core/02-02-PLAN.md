---
phase: 02-react-ui-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Sidebar.tsx
  - src/components/Header.tsx
  - src/components/StatusBar.tsx
autonomous: true
requirements: [NAV-01, DISP-07, DISP-08]

must_haves:
  truths:
    - "Sidebar shows Agents/Skills/Commands with active indicator (accent border)"
    - "Clicking a sidebar item sets activeSection in store and triggers loadSection"
    - "Sidebar uses lucide-react icons for each section"
    - "StatusBar shows scan path (~/.claude), enabled count (green dot), disabled count (grey dot), version"
    - "Header contains SearchBar and FilterPills"
    - "Layout uses CSS grid/flex: sidebar 200px fixed, main content fills rest"
    - "Dark theme colors applied consistently"
  artifacts:
    - src/components/Sidebar.tsx
    - src/components/Header.tsx
    - src/components/StatusBar.tsx
  key_links:
    - "Sidebar reads/writes store.activeSection"
    - "Header composes SearchBar + FilterPills"
    - "StatusBar reads counts from active section data in store"
---

<objective>
Implement the layout shell components: Sidebar navigation, Header with search/filters, and StatusBar.

Purpose: These wrap the page structure. After this plan, the app has a complete layout frame ready for content.
Output: Working layout with sidebar navigation, header, and status bar.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-react-ui-core/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Sidebar, Header, StatusBar</name>
  <files>src/components/Sidebar.tsx, src/components/Header.tsx, src/components/StatusBar.tsx</files>
  <action>
1. Create `src/components/Sidebar.tsx`:
   - Fixed 200px width, full height, dark surface background (#1a1a2e)
   - App title "CAM" or small logo at top
   - Three nav items: Agents, Skills, Commands
   - Each with lucide-react icon (Users, Sparkles, Terminal or similar)
   - Active item has left border accent (#00d4aa) and lighter background
   - Click sets store.activeSection and calls store.loadSection
   - Show count per section from store (agents.length, etc.)

2. Create `src/components/Header.tsx`:
   - Full width, sits above content area
   - Contains SearchBar (left) and FilterPills (right)
   - Pass computed counts to FilterPills from store
   - Thin bottom border (#2a2a44)

3. Create `src/components/StatusBar.tsx`:
   - Full width, sits at bottom
   - Left: scan path (`~/.claude`)
   - Center/Right: enabled count with green dot, disabled count with grey dot
   - Far right: version `v0.1.0`
   - Small font, muted colors
   - Thin top border (#2a2a44)
  </action>
  <verify>
    <automated>cd "C:/Users/User/Desktop/Claude-Agent-Manager" && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>Sidebar navigates between sections, StatusBar shows counts and path, Header composes search/filter</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Sidebar shows 3 sections with icons and active state
3. StatusBar displays scan path, counts with colored dots, version
4. Header contains SearchBar and FilterPills
</verification>

<success_criteria>
- Sidebar navigation between Agents/Skills/Commands works
- StatusBar shows scan path, enabled/disabled counts, version
- Header integrates search and filter primitives
- Layout uses dark theme consistently
</success_criteria>

<output>
After completion, create `.planning/phases/02-react-ui-core/02-02-SUMMARY.md`
</output>
