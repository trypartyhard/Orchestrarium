---
phase: 02-react-ui-core
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/components/AgentCard.tsx
  - src/components/AgentGroup.tsx
  - src/components/AgentList.tsx
  - src/components/EmptyState.tsx
  - src/App.tsx
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03, DISP-04, DISP-05, DISP-06, TOGL-01, TOGL-02]

must_haves:
  truths:
    - "AgentCard shows name, description, color dot, toggle switch, and disabled visual state (opacity 0.5)"
    - "AgentCard shows 'invalid config' badge when invalid_config is true"
    - "AgentGroup is collapsible with group name, item count, and group toggle"
    - "Group toggle flips all items in group, rolls back on partial failure"
    - "AgentList groups items by group field and renders AgentGroup for each"
    - "EmptyState shows helpful message when no agents found or directory missing"
    - "App.tsx wires layout (Sidebar + Header + AgentList + StatusBar) and loads initial section"
    - "Toggle click triggers store.toggleItem with <200ms UI feedback"
    - "Search and filter apply to the displayed list"
  artifacts:
    - src/components/AgentCard.tsx
    - src/components/AgentGroup.tsx
    - src/components/AgentList.tsx
    - src/App.tsx
  key_links:
    - "AgentCard uses Toggle, ColorDot, Badge primitives"
    - "AgentGroup uses Toggle for group-level toggle"
    - "AgentList reads from store and applies search/filter"
    - "App.tsx composes Sidebar, Header, AgentList, StatusBar, Toast"
---

<objective>
Implement the feature components (AgentCard, AgentGroup, AgentList, EmptyState) and wire the complete App.

Purpose: This brings everything together — primitives + layout + feature components = working app.
Output: Complete working UI that displays agent cards, supports toggle, search, filter, and group operations.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-react-ui-core/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AgentCard component</name>
  <files>src/components/AgentCard.tsx</files>
  <action>
1. Create `src/components/AgentCard.tsx`:
   - Props: `item: AgentInfo`
   - Layout: horizontal row with color dot, name+description, toggle on right
   - Name: bold, primary color (#d0d0e8)
   - Description: smaller, secondary color (#555577), truncated to 1 line
   - ColorDot from frontmatter color
   - Toggle switch on far right
   - When disabled: entire card at opacity 0.5
   - When invalid_config: show Badge with "invalid config" in red
   - Surface background (#1a1a2e) with hover (#22223a)
   - Rounded corners, subtle border (#2a2a44)
   - Toggle click calls store.toggleItem(item)
  </action>
  <verify>
    <automated>cd "C:/Users/User/Desktop/Claude-Agent-Manager" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>AgentCard renders with all visual states: enabled, disabled (opacity), invalid config badge</done>
</task>

<task type="auto">
  <name>Task 2: Implement AgentGroup with group toggle</name>
  <files>src/components/AgentGroup.tsx</files>
  <action>
1. Create `src/components/AgentGroup.tsx`:
   - Props: `groupName: string`, `items: AgentInfo[]`
   - Collapsible: click header to expand/collapse (default expanded)
   - Header shows: group name (uppercase), item count, group toggle
   - Group toggle: if all enabled → disable all, otherwise → enable all
   - Group toggle calls store.toggleItem for each item sequentially
   - On partial failure: items that failed revert (handled by store's per-item error handling)
   - Renders AgentCard for each item when expanded
   - Chevron icon (lucide-react ChevronDown/ChevronRight) for collapse state
  </action>
  <verify>
    <automated>cd "C:/Users/User/Desktop/Claude-Agent-Manager" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>AgentGroup collapses/expands, group toggle flips all items, shows count</done>
</task>

<task type="auto">
  <name>Task 3: Implement AgentList, EmptyState, and wire App.tsx</name>
  <files>src/components/AgentList.tsx, src/components/EmptyState.tsx, src/App.tsx</files>
  <action>
1. Create `src/components/EmptyState.tsx`:
   - Props: `section: string`
   - Shows icon (FolderOpen from lucide-react) and message
   - Message: "No {section} found" with hint about ~/.claude/{section}/ directory
   - Muted colors, centered

2. Create `src/components/AgentList.tsx`:
   - Reads store: items from active section, searchQuery, filter
   - Apply search filter: case-insensitive match on name + description
   - Apply status filter: all/enabled/disabled
   - Group items by `group` field
   - Sort groups: named groups alphabetically, "Custom" last
   - Render AgentGroup for each group
   - If no items after filtering: show EmptyState
   - If loading: show simple loading indicator
   - Scrollable overflow

3. Update `src/App.tsx`:
   - Layout: flex, full viewport height
   - Left: Sidebar (fixed width)
   - Right: flex column — Header, AgentList (flex-grow, scroll), StatusBar
   - Toast component rendered at root level (fixed position)
   - On mount: load initial section (agents)
   - Dark background (#12121c) on body/root
  </action>
  <verify>
    <automated>cd "C:/Users/User/Desktop/Claude-Agent-Manager" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>App renders full layout, AgentList groups and filters items, EmptyState shows when needed</done>
</task>

<task type="auto">
  <name>Task 4: Verify full build</name>
  <files>src/App.tsx</files>
  <action>
1. Run `npx tsc --noEmit` — must pass with no errors
2. Run `npm run build` — Vite build must succeed
3. Verify all components are imported and used in App.tsx
4. Verify store exports are correct
5. Fix any remaining TypeScript errors
  </action>
  <verify>
    <automated>cd "C:/Users/User/Desktop/Claude-Agent-Manager" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Full TypeScript + Vite build succeeds, all components wired in App.tsx</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. AgentCard shows name, description, color dot, toggle, disabled state, invalid badge
4. AgentGroup collapses/expands, group toggle works
5. AgentList groups items and applies search/filter
6. EmptyState renders when no items
7. App.tsx has complete layout with all components
</verification>

<success_criteria>
- AgentCard displays all required info with correct visual states
- AgentGroup collapsible with group-level toggle
- AgentList groups, searches, and filters items
- EmptyState for missing directories
- App.tsx wires everything into working layout
- Full build (tsc + vite) succeeds
- All 15 Phase 2 requirements addressed
</success_criteria>

<output>
After completion, create `.planning/phases/02-react-ui-core/02-03-SUMMARY.md`
</output>
